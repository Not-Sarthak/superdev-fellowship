services:
  - type: web
    name: axum-solana-server
    env: rust
    plan: free
    buildCommand: cargo build --release
    startCommand: ./target/release/axum-server
    envVars:
      - key: PORT
        value: 10000
      - key: RUST_LOG
        value: info 